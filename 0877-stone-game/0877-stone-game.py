class Solution:
    def stoneGame(self, piles: List[int]) -> bool:
        # alice, bob = 0, 0 

        # i, j = 0, len(piles) - 1

        # while i < j: 
        #     if piles[i] > piles[j]: 

        return True